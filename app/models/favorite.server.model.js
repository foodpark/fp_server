var knex = require('../../config/knex');

/** var FavoriteSchema = new Schema({
    _id: false,
    created: {
        type: Date,
        default: Date.now
    },
    unit: {
        type: Schema.ObjectId,
        ref: 'Unit'
    },
    user: {
        type: Schema.ObjectId,
        ref: 'User'
    }
}); **/
